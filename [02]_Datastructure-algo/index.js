// Data Structure

// 1.1 : Use an Array to Store a Collection of Data

/*
        - you have one dimentional array and multie  dimenstional 
        - in data structure the you should have the ability to access the data that is stored in the array 
*/

// 1.2  Add Items to an Array with push() and unshift()

// Remove Items from an Array with pop() and shift()

// Remove Items Using splice() //

// Copy Array Items Using slice()

// Copy an Array with the Spread Operator

// Combine Arrays with the Spread Operator

// Check For The Presence of an Element With indexOf()

// Iterate Through All an Array's Items Using For Loops

/* 
- ways used also to iterate over an array . 

such as every(), forEach(), map(), etc.), however the technique which is most flexible and offers us the greatest amount of control is a simple for loop.

  */
// deepth in array how to access x_Dimentional array
// Add Key-Value Pairs to JavaScript Objects

// Access Property Names with Bracket Notation

// Use the delete Keyword to Remove Object Properties

//Check if an Object has a Property

// Check if an Object has a Property

/*
 users.hasOwnProperty('Alan');
 'Alan' in users;

*/

//Iterate Through the Keys of an Object with a for...in Statement

/* 
       here the best usage for for in statment 
*/
//Generate an Array of All Object Keys with Object.keys()
